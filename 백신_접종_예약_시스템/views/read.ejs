<!DOCTYPE html>
<html>
    <head>
        <script src="/javascripts/jquery-3.6.0.min.js"></script>
        <title><%= title %></title>
        <link rel='stylesheet' href='/stylesheets/style.css'/>
    </head>
    <body>
        <h1><%= title %></h1>
        <form action="/prj1/update" method="get">
            <table border="1">
                <input type="hidden" name="idx" value="<%=row.idx%>"/>
                <tr>
                    <td>작성자</td>
                    <td><%=row.creator_id%></td>
                </tr>
                <tr>
                    <td>제목</td>
                    <td><%=row.title%></td>
                </tr>
                <tr>
                    <td>내용</td>
                    <td><%=row.content%></td>
                </tr>
                <tr>
                    <td>조회수</td>
                    <td><%=row.hit%></td>
                </tr>
                <tr>
                    <td>이미지</td>
                    <%console.log(row.image);%>
                    <% if(row.image == null) {%>
                    <td>이미지가 없습니다.</td>
                    <% } else{%>
                    <td><img src="../../images/<%=row.image%>"></td>
                    <% } %>
                </tr>
                <tr>
                    <td colspan="2">
                        <button type="submit">글 수정</button>
                        <a href="/prj1">리스트로 돌아가기</a>
                    </td>
                </tr>
            </table>
        </form>
    </body>
</html>